import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Brand ───────────────────────────────────────────────────────────────────
  static const primary      = Color(0xFF4A90E2);
  static const primaryDark  = Color(0xFF2C5F8A);
  static const primaryLight = Color(0xFFEBF3FD);
  static const navy         = Color(0xFF1A2E4A);
  static const gold        = Color(0xFFD4A017);
  static const amber       = Color(0xFFF59E0B);
  static const streak      = Color(0xFFFF6B35);
  static const mana        = Color(0xFF1CB0F6);
  static const surface     = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF1F2937);
  static const textSecond  = Color(0xFF6B7280);
  static const border      = Color(0xFFE5E7EB);

  // ── Light Theme Backgrounds ────────────────────────────────────────────────
  static const background      = Color(0xFFF0F7FF);
  static const headerBg        = Color(0xFFFFFFFF);
  static const bottomNavBg     = Color(0xFFFFFFFF);

  // ── Dark Theme Backgrounds ────────────────────────────────────────────────
  static const darkBackground  = Color(0xFF131F24);
  static const darkSurface     = Color(0xFF1C2B33);
  static const darkBorder      = Color(0xFF2A3A44);
  static const darkTextPrimary = Color(0xFFEEF2F7);
  static const darkTextSecond  = Color(0xFF8BA0AE);

  // ── Gamification ───────────────────────────────────────────────────────────
  static const Color xpColor = Color(0xFFFFCC00);
  static const Color streakColor = Color(0xFFFF6B35);
  static const Color heartColor = Color(0xFFFF4B4B);
  static const Color manaColor = Color(0xFF1CB0F6);

  // ── Node Colors (Duolingo-inspired) ────────────────────────────────────────
  static const Color nodeCompleted      = Color(0xFF58CC02);
  static const Color nodeCompletedDark  = Color(0xFF45A800);
  static const Color nodeCurrent        = Color(0xFF1CB0F6);
  static const Color nodeCurrentDark    = Color(0xFF0B8FCB);
  static const Color nodeLocked         = Color(0xFFE5E5E5);
  static const Color nodeLockedDark     = Color(0xFFAFAFAF);
  static const Color nodeLockedDarkMode = Color(0xFF3A4A54);

  // ── Feedback ───────────────────────────────────────────────────────────────
  static const Color correct = Color(0xFF58CC02);
  static const Color incorrect = Color(0xFFFF4B4B);
  static const Color correctLight = Color(0xFFD7FFB8);
  static const Color incorrectLight = Color(0xFFFFDFE0);
  static const Color correctDark = Color(0xFF46A302);
  static const Color incorrectDark = Color(0xFFCC3B3B);

  // ── Leagues ────────────────────────────────────────────────────────────────
  static const Color bronze = Color(0xFFCD7F32);
  static const Color silver = Color(0xFFC0C0C0);
  static const Color goldLeague = Color(0xFFFFD700);
  static const Color sapphire = Color(0xFF0F52BA);
  static const Color onyx = Color(0xFF353839);
  static const Color diamond = Color(0xFFB9F2FF);

  // ── Neutrals ───────────────────────────────────────────────────────────────
  static const Color textSecondary = Color(0xFF777777);
  static const Color textHint = Color(0xFFAFAFAF);
  static const Color shadow = Color(0x1A000000);
  static const Color overlay = Color(0x80000000);

  // ── Button shadow (Duolingo-style bottom shadow) ───────────────────────────
  static const Color buttonShadowGreen = Color(0xFF46A302);
  static const Color buttonShadowBlue = Color(0xFF2C5F8A);
  static const Color buttonShadowRed = Color(0xFFCC3B3B);

  // ── Neumorphism ────────────────────────────────────────────────────────────
  static const Color neumorphBase = Color(0xFFE8EDF2);
  static const Color neumorphLight = Color(0xFFFFFFFF);
  static const Color neumorphDark = Color(0xFFC8D0D8);
  static const Color neumorphInnerLight = Color(0xFFD6DCE4);
  static const Color neumorphInnerDark = Color(0xFFF8FBFF);
}
