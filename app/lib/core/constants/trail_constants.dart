/// Centralized visual constants for the trail/path on the Home screen.
/// These are PURELY VISUAL — no business logic here.
class TrailConstants {
  TrailConstants._();

  // ── Node Sizes ──────────────────────────────────────────────────────────────
  static const double nodeSize         = 76.0;
  static const double nodeIconSize     = 30.0;
  static const double nodeShadowHeight = 5.0;

  // ── Stage Ring ──────────────────────────────────────────────────────────────
  static const double ringWidth        = 6.0;
  static const double ringGap          = 0.18;
  static const double ringPadding      = 4.0;

  // ── Path / Trail ────────────────────────────────────────────────────────────
  static const double nodeVerticalSpacing = 112.0;
  static const double pathStrokeWidth  = 5.0;
  static const double dashLength       = 8.0;
  static const double gapLength        = 10.0;
  static const double zigzagAmplitude  = 0.22;

  // ── Bottom Nav ──────────────────────────────────────────────────────────────
  static const double navBarHeight     = 72.0;
  static const double navIconSize      = 28.0;
  static const double navDotSize       = 5.0;

  // ── Top Bar ─────────────────────────────────────────────────────────────────
  static const double topBarHeight     = 60.0;
  static const double topBarIconSize   = 50.0;
  static const double statFontSize     = 19.0;
  static const double statEmojiSize    = 28.0;
}
